# Environment variables
NODE_ENV=production

# Gateway Authentication Token (OPENCLAW_GATEWAY_TOKEN)
OPENCLAW_GATEWAY_TOKEN=MySecretToken123

# Custom LLM Configuration
# Base URL for your model provider (e.g. local Ollama, vLLM, or DeepSeek)
LLM_BASE_URL=https://api.openai.com/v1
# API Key for the model provider
LLM_API_KEY=sk-your-api-key-here

# Config path for Docker/Manual usage
# Relative to working directory (Docker: /app, Manual: Project Root)
OPENCLAW_CONFIG_PATH=config/openclaw.json